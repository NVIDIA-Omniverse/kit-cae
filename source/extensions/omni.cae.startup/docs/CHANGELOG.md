# Changelog

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [1.0.0]

### Added
- Initial release.
- `exts."omni.cae.startup".usdFile` setting: when set, the extension waits for RTX to deliver
  its first frame on the startup stage and then closes it and opens the specified USD file.
